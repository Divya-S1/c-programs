#include<stdio.h>
#include<math.h>
void main()
    {   
      double sin value,cos value,x;
      printf("\n Enter the value:");
      scanf("%f",&x);
      sin value =sin(x);
      cos value =cos(x);
      printf("\n the sin value of %f=%f",x,sin value);
      printf("\n the cos value of %f=%f",x,cos value);
    }  
